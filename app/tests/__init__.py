"""
Integration tests package.

These tests exercise complete user flows across multiple apps
(accounts, repositories, explore) via the Django test client,
as opposed to the per-app unit tests in each app's tests.py.
"""
